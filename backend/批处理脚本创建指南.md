# Windows批处理脚本创建指南

## 📋 需要创建的文件

你需要在 `backend/` 目录下创建以下两个文件：

1. **安装依赖.bat** - 自动安装所有Python依赖包
2. **启动应用.bat** - 一键启动MusicAgent桌面应用

---

## 📝 创建方法

### 方法一：使用记事本（推荐新手）

#### 创建"安装依赖.bat"

1. **打开记事本**：
   - 按 `Win + R`，输入 `notepad`，回车

2. **复制以下内容** 到记事本：

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo    MusicAgent 依赖安装脚本
echo ========================================
echo.

REM 检查Python是否安装
python --version >nul 2>&1
if %errorlevel% neq 0 (
    echo [错误] 未检测到Python！
    echo 请先安装Python 3.8或更高版本
    echo 下载地址: https://www.python.org/
    pause
    exit /b 1
)

echo [信息] 检测到Python版本:
python --version
echo.

REM 检查是否在backend目录
if not exist "requirements.txt" (
    echo [警告] 未找到requirements.txt文件
    echo 请确保在backend目录下运行此脚本
    echo.
    pause
    exit /b 1
)

echo [信息] 开始安装依赖包...
echo.

REM 升级pip
echo [步骤1/2] 升级pip到最新版本...
python -m pip install --upgrade pip
echo.

REM 安装依赖
echo [步骤2/2] 安装项目依赖...
pip install -r requirements.txt
echo.

if %errorlevel% equ 0 (
    echo ========================================
    echo    安装完成！
    echo ========================================
    echo.
    echo 已安装的核心包:
    echo   - openai (DeepSeek API)
    echo   - PyQt5 (桌面应用界面)
    echo   - music21 (音乐处理)
    echo   - mido (MIDI文件处理)
    echo   - flask (Web后端)
    echo.
    echo 提示: 请配置DeepSeek API密钥后再运行应用
    echo.
) else (
    echo ========================================
    echo    安装失败！
    echo ========================================
    echo.
    echo 请检查:
    echo 1. 网络连接是否正常
    echo 2. 是否有管理员权限
    echo 3. Python版本是否符合要求
    echo.
)

pause
```

3. **保存文件**：
   - 点击 `文件` → `另存为`
   - 文件名输入：`安装依赖.bat`
   - 保存类型选择：`所有文件 (*.*)`
   - 编码选择：`UTF-8`
   - 保存位置：`MusicAgent/backend/` 目录

#### 创建"启动应用.bat"

1. **新建记事本文件**

2. **复制以下内容**：

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo    MusicAgent 启动脚本
echo ========================================
echo.

REM 检查Python是否安装
python --version >nul 2>&1
if %errorlevel% neq 0 (
    echo [错误] 未检测到Python！
    echo 请先安装Python 3.8或更高版本
    pause
    exit /b 1
)

REM 检查gui_app.py是否存在
if not exist "gui_app.py" (
    echo [错误] 未找到gui_app.py文件
    echo 请确保在backend目录下运行此脚本
    pause
    exit /b 1
)

REM 检查依赖是否安装
python -c "import PyQt5" >nul 2>&1
if %errorlevel% neq 0 (
    echo [警告] 依赖包未安装！
    echo.
    echo 是否现在安装依赖？(Y/N)
    set /p choice=请选择: 
    if /i "%choice%"=="Y" (
        echo.
        echo 正在安装依赖...
        call 安装依赖.bat
        echo.
    ) else (
        echo.
        echo 请先运行"安装依赖.bat"安装依赖包
        pause
        exit /b 1
    )
)

echo [信息] 正在启动MusicAgent桌面应用...
echo.
echo ----------------------------------------
echo  提示:
echo  1. 请确保已配置DeepSeek API密钥
echo  2. 使用前请先安装并打开Sonic Pi
echo  3. 生成的MIDI文件保存在midi_output目录
echo ----------------------------------------
echo.

REM 启动应用
python gui_app.py

if %errorlevel% neq 0 (
    echo.
    echo ========================================
    echo    应用异常退出
    echo ========================================
    echo.
    echo 可能的原因:
    echo 1. API密钥未配置或无效
    echo 2. 依赖包安装不完整
    echo 3. 网络连接问题
    echo.
    echo 请检查上方的错误信息
    echo.
)

pause
```

3. **保存文件**：
   - 文件名：`启动应用.bat`
   - 保存类型：`所有文件 (*.*)`
   - 编码：`UTF-8`
   - 位置：`MusicAgent/backend/` 目录

---

### 方法二：使用VS Code（推荐开发者）

1. **打开VS Code**

2. **在backend目录创建文件**：
   - 右键 `backend` 文件夹 → `新建文件`
   - 输入文件名：`安装依赖.bat`

3. **粘贴代码**（从上面复制）

4. **保存**：`Ctrl + S`

5. **重复步骤** 创建 `启动应用.bat`

---

## ⚠️ 重要注意事项

### 编码问题

**必须使用UTF-8编码保存！** 否则中文会乱码。

在记事本中：
1. `文件` → `另存为`
2. 底部"编码"下拉框选择 `UTF-8`
3. 点击"保存"

### 文件位置

两个.bat文件必须放在 `backend/` 目录下，与 `gui_app.py` 和 `requirements.txt` 在同一目录。

正确的目录结构：
```
MusicAgent/
└── backend/
    ├── gui_app.py
    ├── app.py
    ├── requirements.txt
    ├── 安装依赖.bat      ← 新创建
    ├── 启动应用.bat      ← 新创建
    └── ...
```

---

## 🧪 测试脚本

### 测试"安装依赖.bat"

1. 打开 `MusicAgent/backend/` 目录
2. 双击 `安装依赖.bat`
3. 应该看到：
   ```
   ========================================
      MusicAgent 依赖安装脚本
   ========================================
   
   [信息] 检测到Python版本:
   Python 3.x.x
   
   [信息] 开始安装依赖包...
   ...
   ```

4. 等待安装完成

### 测试"启动应用.bat"

1. 在 `backend/` 目录双击 `启动应用.bat`
2. 应该看到：
   ```
   ========================================
      MusicAgent 启动脚本
   ========================================
   
   [信息] 正在启动MusicAgent桌面应用...
   ```

3. MusicAgent GUI窗口应该打开

---

## 🐛 常见问题

### Q: 双击.bat文件一闪而过

**A:** 文件路径或依赖有问题。右键 `.bat` 文件 → `编辑`，在最后一行 `pause` 前加一行 `echo [等待调试]`，然后双击查看错误信息。

### Q: 提示"不是内部或外部命令"

**A:** Python未添加到系统PATH。重新安装Python时勾选 "Add Python to PATH"。

### Q: 显示乱码

**A:** 文件编码不是UTF-8。重新用UTF-8编码保存。

### Q: 提示找不到requirements.txt

**A:** .bat文件不在backend目录，或者在错误的目录运行了脚本。

---

## 📤 上传到GitHub

创建完成后：

```bash
cd MusicAgent
git add backend/安装依赖.bat backend/启动应用.bat
git commit -m "添加Windows批处理脚本：自动安装依赖和启动应用"
git push
```

---

## ✅ 验证清单

上传前检查：

- [ ] 两个.bat文件都在 `backend/` 目录
- [ ] 文件使用UTF-8编码保存
- [ ] 文件名正确：`安装依赖.bat` 和 `启动应用.bat`
- [ ] 已在本地测试过，可以正常运行
- [ ] 没有硬编码的个人路径或API密钥

---

## 📝 README更新建议

上传脚本后，在README中添加：

```markdown
### 第四步：安装项目依赖

**进入backend目录：**
```bash
cd backend
```

**方法A - 自动安装（Windows推荐）：**
```bash
# 双击运行
安装依赖.bat
```

**方法B - 手动安装：**
```bash
pip install -r requirements.txt
```
```

这样用户在Windows上可以更方便地使用你的项目！
